# 대시보드가 조회할 내부 API 주소
VITE_RANKING_API_URL=http://localhost:8787

# 서버 전용 값 — VITE_ 접두사를 붙이지 마세요.
DATABASE_URL=
SCREENSHOT_STORAGE_PATH=./screenshots
COLLECTION_TIMEZONE=Asia/Seoul

# 새로 만든 본인 소유 Apps Script 웹 앱 주소 — 실제 값은 .env에만 입력
COLLECTOR_GAS_WEB_APP_URL=
COLLECTOR_SHARED_SECRET=
COLLECTOR_DRY_RUN=0
