# DESIGN — 네이버 경쟁사 검색 순위 분석 대시보드

## 1. 제품 정의

키워드별 네이버 검색 결과에서 보험사 광고 노출 순위를 시간 단위로 수집하고, 광고 운영 담당자가 경쟁사의 입찰·노출 패턴을 빠르게 비교하는 내부 분석 도구다.

- 핵심 사용자: 퍼포먼스 마케터, 광고 운영자, 광고주 보고 담당자
- 핵심 질문: “어떤 회사가 어떤 키워드·디바이스·시간대에서 우위를 점했는가?”
- 기본 사용 흐름: 필터 선택 → 순위 흐름 확인 → 업체 집중 분석 → 시간별 원자료/증빙 확인
- 화면 성격: 설명형 랜딩 페이지가 아닌 고밀도 분석 작업 화면

## 2. 정보 구조

### 상단 헤더

- Eyebrow: `AI RANKING INTELLIGENCE`
- 제목: `타사 순위 분석 솔루션 (AI)`
- 설명: 최근 경쟁사 순위 입찰 전략, 점유율 변화, 운영 특이사항 분석
- 우측에는 마지막 업데이트 시각과 수집 상태를 배치한다.

### 전역 필터 바

- 키워드: 자동차보험, 다이렉트자동차보험, 비교견적사이트, 자동차보험비교
- 디바이스: 통합, PC, Mobile
- 월: YYYY-MM
- 집중 분석 업체: 선택 또는 전체
- 필터는 스크롤 중에도 접근할 수 있도록 상단 고정 영역으로 동작한다.

### 주요 분석 영역

1. 요약 KPI
   - 평균 순위
   - 1위 점유율
   - 수집 성공률
   - 분석 표본 수
2. AI 운영 전략 및 패턴 요약
3. 일자별 평균 순위 흐름
4. 업체별 평균 순위 및 점유율 비교
5. 시간대별 플레이 분석
6. 평일·주말 운영 비교
7. 일자별 상세 데이터 표
8. 24시간 상세 랭킹보드 모달

## 3. 시각 언어

### 색상 토큰

| 역할 | 값 | 용도 |
| --- | --- | --- |
| Navy 950 | `#0B1930` | 헤더, 핵심 텍스트 |
| Slate 700 | `#334155` | 보조 텍스트 |
| Slate 500 | `#64748B` | 메타 정보 |
| Slate 200 | `#E2E8F0` | 테두리 |
| Slate 50 | `#F8FAFC` | 페이지 배경 |
| Blue 600 | `#2563EB` | 선택 상태, 주요 액션 |
| Cyan 500 | `#06B6D4` | 데이터 강조, 보조 포인트 |
| Emerald 500 | `#10B981` | 정상 수집, 개선 |
| Rose 500 | `#F43F5E` | 실패, 순위 악화 |
| Amber 400 | `#FBBF24` | 주의, 부분 수집 |

### 업체 색상

| 업체 | 색상 |
| --- | --- |
| 삼성 | `#0057D9` |
| KB | `#FFBC00` |
| DB | `#008D62` |
| 현대 | `#FF7000` |
| AXA | `#00008F` |
| 캐롯 | `#E60000` |
| 네이버페이 | `#03C75A` |

### 타이포그래피

- 기본 글꼴: Pretendard, Apple SD Gothic Neo, Noto Sans KR, sans-serif
- 숫자: `font-variant-numeric: tabular-nums`
- 화면 제목: 28–32px / 800
- 섹션 제목: 18–20px / 750
- 카드 지표: 24–30px / 800
- 표 본문: 12–13px / 500
- 보조 설명: 12–14px / 500

### 표면과 간격

- 페이지 배경은 옅은 청회색, 카드 배경은 흰색
- 카드 반경 16px, 내부 패널 반경 12px
- 카드 테두리 1px `Slate 200`, 그림자는 매우 약하게 사용
- 기본 간격 단위 4px, 주요 섹션 간격 24px
- 분석 화면 특성상 첫 화면에서 필터와 핵심 차트가 함께 보여야 한다.

## 4. 차트 규칙

- 순위 축은 1위가 위에 오도록 Y축을 역전한다.
- 업체별 선 색상은 항상 동일하게 유지한다.
- 선 그래프는 완만한 곡선으로 표시하되 실제 측정점을 숨기지 않는다.
- 결측치는 0위로 처리하지 않고 선을 끊는다.
- 툴팁에는 시각, 업체, 순위, 디바이스, 수집 상태를 표시한다.
- 비교 기간은 실선(현재), 점선(이전)으로 구분한다.
- 색상만으로 상태를 구분하지 않고 라벨·아이콘을 함께 사용한다.

## 5. 표 규칙

- 헤더 고정, 첫 번째 열 고정
- 순위는 낮은 숫자가 좋은 값임을 헤더에서 명시
- 등락은 `▲ 악화`, `▼ 개선`, `– 동일`로 표기
- 데이터가 없으면 `미수집`, 수집 오류이면 `실패`로 분리
- 500행 이상에서는 가상 스크롤 또는 페이지네이션 사용
- Excel 내보내기는 현재 필터와 전체 데이터 중 선택 가능하게 한다.

## 6. 상호작용

- 업체명을 누르면 모든 차트와 표가 해당 업체 기준으로 집중 표시된다.
- 같은 업체를 다시 누르거나 `전체 보기`를 누르면 집중 모드가 해제된다.
- 필터 변경은 즉시 화면에 반영되며 URL 쿼리에도 상태를 보존한다.
- 로딩, 빈 결과, 부분 성공, 전체 실패 상태를 각각 별도로 제공한다.
- 내부 수집 화면과 광고주용 보고 화면은 경로 및 권한을 분리한다.

## 7. 반응형

- Desktop 1440px 이상: 12열 그리드, 차트 2열 구성
- Laptop 1024–1439px: 8열 그리드, 복잡한 표는 가로 스크롤
- Tablet 768–1023px: 카드 1열, 필터 2열
- Mobile 767px 이하: 핵심 KPI·요약·업체 전환을 우선하고 상세 표는 별도 드로어로 표시
- 터치 대상 최소 높이 44px

## 8. 접근성

- 모든 필터에 명시적인 label 제공
- 키보드만으로 필터, 업체 선택, 모달 닫기 가능
- 포커스 링은 `Blue 600` 2px
- 차트 핵심 수치는 표 형태의 대체 정보도 제공
- 상태 배지는 텍스트를 포함하고 최소 WCAG AA 대비 유지

## 9. 데이터 모델

### RankingObservation

```ts
type RankingObservation = {
  id: string;
  keyword: string;
  device: "PC" | "MOBILE";
  observedAt: string;
  company: string;
  rank: number | null;
  placement: "POWER_LINK" | "OTHER";
  screenshotPath: string | null;
  status: "SUCCESS" | "PARTIAL" | "FAILED";
  collectorVersion: string;
};
```

### CollectionRun

```ts
type CollectionRun = {
  id: string;
  scheduledAt: string;
  startedAt: string | null;
  completedAt: string | null;
  requested: number;
  succeeded: number;
  failed: number;
  status: "QUEUED" | "RUNNING" | "PARTIAL" | "FINAL" | "FAILED";
  message: string | null;
};
```

## 10. 보안 및 배포 원칙

- API 키, 세션, 쿠키, 프록시 정보는 브라우저 코드에 포함하지 않는다.
- 공개 대시보드는 읽기 전용 집계 데이터만 받는다.
- 원본 스크린샷은 인증된 내부 사용자만 접근한다.
- 수집기와 대시보드 서버를 분리하고 최소 권한을 부여한다.
- 이미 완료된 관측값은 수정하지 않고 재수집 결과는 새 버전으로 남긴다.
- GitHub 공개판에는 실제 경쟁사 데이터, 이메일, 내부 엔드포인트를 포함하지 않는다.

## 11. 구현 파일 순서

1. `DESIGN.md` — 본 설계 기준
2. `package.json` — 실행·검증 명령
3. `src/types.ts` — 데이터 계약
4. `src/config.ts` — 키워드·업체·시간대 설정
5. `src/data/sample.ts` — 공개 가능한 예시 데이터
6. `src/lib/analytics.ts` — 평균 순위·점유율 계산
7. `src/components/*` — 필터, KPI, 차트, 표
8. `src/App.tsx` — 화면 조합과 상태
9. `collector/*` — 시간별 수집 및 스크린샷 인터페이스
10. `server/*` — 저장·조회 API
11. `README.md` — 설치, 운영, 보안, 배포 안내

이 문서는 이후 구현의 단일 기준이며, 시각적 변경이나 데이터 계약 변경 시 코드보다 먼저 갱신한다.
